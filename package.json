{
  "name": "will-github-run-my-cron",
  "private": true,
  "type": "module",
  "description": "Scheduled GitHub Actions workflows, measured every hour: how late they run and how often they do not run at all.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "collect": "node scripts/collect.mjs",
    "ci": "npm test"
  }
}
