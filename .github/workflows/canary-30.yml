# Canary :30 -- asks GitHub to run this every hour at minute 30 and does nothing else.
# The measurement is not what this job prints; it is when GitHub creates the run, read back
# from the Actions API by scripts/collect.mjs and compared with the minute it was asked for.
name: "canary :30"
on:
  schedule:
    - cron: '30 * * * *'
  workflow_dispatch:
permissions: {}
jobs:
  tick:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "canary :30 ran at $(date -u +%FT%TZ) for schedule '${{ github.event.schedule }}' (event ${{ github.event_name }})"
